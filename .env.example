# Frontend Environment Variables
# These are automatically set in docker-compose.yml
# Only needed if running frontend outside Docker

PUBLIC_API_BASE=http://localhost:8000
PUBLIC_API_BASE_SSR=http://localhost:8000
